version: "3"

services:
  qna:
    build: .
    ports:
      - "31000:31000"
    network_mode: host
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 2
              capabilities: [gpu]